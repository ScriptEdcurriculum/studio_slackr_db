{
  "rules": {
    "channels": {
      ".read": "auth != null",
      "$channel": {
        "messages": {
          ".write": "auth != null"
        },
        "users": {
          "$uid": {
            ".write": "auth.uid === $uid"
          }
        }
      }
    }
  }
}
