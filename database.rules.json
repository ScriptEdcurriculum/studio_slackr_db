{
  "rules": {
    "$channel": {
      ".read": "auth != null",
      "messages": {
        ".write": "auth != null"
      },
      "users": {
        "$uid": {
          ".write": "auth.uid === $uid"
        }
      }
    }
  }
}
